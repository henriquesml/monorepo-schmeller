{
  "name": "@euax-ui/components",
  "version": "1.0.5",
  "main": "dist/index",
  "types": "dist/index",
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "yarn run build",
    "test": "yarn run build"
  },
  "peerDependencies": {
    "react": "~17.0.0"
  },
  "dependencies": {
    "@euax-ui/foo": "^1.0.5"
  },
  "devDependencies": {
    "@types/react": "~16.9.43"
  },
  "gitHead": "3bf93f91b640b41c62beac090b5191ad1c84ee97"
}
