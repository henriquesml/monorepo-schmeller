{
  "name": "@euax-ui/components",
  "version": "1.0.7",
  "main": "dist/index",
  "types": "dist/index",
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "yarn run build",
    "test": "yarn run build"
  },
  "peerDependencies": {
    "react": "~17.0.0"
  },
  "dependencies": {
    "@euax-ui/foo": "^1.0.7"
  },
  "devDependencies": {
    "@types/react": "~16.9.43"
  },
  "gitHead": "07245b4a94e04223bccb1d9b69dd0f4556d10882"
}
