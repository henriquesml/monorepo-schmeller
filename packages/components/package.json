{
  "name": "@monorepo-schmeller/components",
  "version": "1.0.16",
  "main": "dist/index",
  "types": "dist/index",
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "yarn run build",
    "test": "yarn run build",
    "pub": "npm publish"
  },
  "peerDependencies": {
    "react": "~17.0.0"
  },
  "dependencies": {
    "@monorepo-schmeller/foo": "^1.0.16"
  },
  "devDependencies": {
    "@types/react": "~16.9.43"
  },
  "publishConfig": {
    "access": "public"
  }
}
