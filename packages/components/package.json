{
  "name": "@euax-ui/components",
  "version": "1.0.2",
  "main": "dist/index",
  "types": "dist/index",
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "yarn run build",
    "test": "yarn run build"
  },
  "peerDependencies": {
    "react": "~17.0.0"
  },
  "dependencies": {
    "@euax-ui/foo": "^1.0.2"
  },
  "devDependencies": {
    "@types/react": "~16.9.43"
  },
  "gitHead": "e384899e68cb187212f855c5b28fff47f8e7e1a9"
}
