{
  "name": "@euax-ui/components",
  "version": "1.0.4",
  "main": "dist/index",
  "types": "dist/index",
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "yarn run build",
    "test": "yarn run build"
  },
  "peerDependencies": {
    "react": "~17.0.0"
  },
  "dependencies": {
    "@euax-ui/foo": "^1.0.4"
  },
  "devDependencies": {
    "@types/react": "~16.9.43"
  },
  "gitHead": "7e878cea9bff46ff4fe455342f0ab5a04a4b7ba2"
}
