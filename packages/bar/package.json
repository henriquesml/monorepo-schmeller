{
  "name": "@monorepo-ts/bar",
  "version": "1.0.12",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "yarn run build",
    "test": "yarn run build"
  },
  "dependencies": {
    "@monorepo-ts/foo": "^1.0.12"
  },
  "devDependencies": {
    "rimraf": "~3.0.2",
    "typescript": "~4.1.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "gitHead": "bd08d6f08aabcdf6f69b53bab09f437a301dcf8d"
}
