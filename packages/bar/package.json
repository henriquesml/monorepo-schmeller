{
  "name": "@monorepo-schmeller/bar",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "yarn run build",
    "test": "yarn run build",
    "pub": "npm publish --access public"
  },
  "dependencies": {
    "@monorepo-schmeller/foo": "^1.0.1"
  },
  "devDependencies": {
    "rimraf": "~3.0.2",
    "typescript": "~4.1.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "gitHead": "7d66d3dca0d813ffb78cb078ab2734efcc6210a0"
}
